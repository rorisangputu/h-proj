type Props = {
  hotelId: string;
  pricePerNight: number;
};

const GuestInfo = ({ hotelId, pricePerNight }: Props) => {
    
    return (
        <div>
            
      </div>
  );
};

export default GuestInfo;
